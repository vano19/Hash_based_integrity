#ifndef DEFINITIONS_H
#define DEFINITIONS_H

#include <QString>

extern void callMessage(const QString& text);
extern void callError(const QString& text);
extern void callDone(const QString& text);

#endif // DEFINITIONS_H
